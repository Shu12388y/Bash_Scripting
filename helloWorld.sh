#!bin/bash
echo "hello world !"

#we can also use bash command in here 

ls
# list of files and folders in the dir.

pwd
# present working dir.

npm init -y
# we can also write command that we want to execute